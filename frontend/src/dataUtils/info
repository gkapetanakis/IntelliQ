paths.js is a file which provides an interface
for the URL requests we wish to do.

stores.js keeps the data stored in our Web Browser
when the app is running. It provides a management
interface for our whole app.

constantValues.js is a central place for constants used
in our app